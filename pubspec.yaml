name: responsive_window
description: A lightweight app-level responsive window utility for Flutter, with Material Design 3-inspired width breakpoints.
version: 2.1.2
homepage: https://github.com/gabriel-x-duarte/responsive_window
repository: https://github.com/gabriel-x-duarte/responsive_window
issue_tracker: https://github.com/gabriel-x-duarte/responsive_window/issues

topics:
  - responsive
  - layout
  - adaptive
  - breakpoints
  - widgets

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
